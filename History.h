#ifndef HISTORY_H
#define HISTORY_H

#include "DatabaseEngine.h"
#include <string>

// Standalone procedural function declaration
void displayUserHistory(const DataManager &dm, const std::string &customerId);

#endif // HISTORY_H